const intToText = num => {
    var str = toString(num);
    var len = str.length
    if (len <= 3) return num;
    else{
        if(str[1] =='0'){
            switch (len) {
                case value:
                    
                    break;
            
                default:
                    break;
            }

        }
        var first = str[0];
        var second = str[1];
    }
}

export default intToText;